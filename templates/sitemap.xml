<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <!-- === PÁGINAS PRINCIPALES === -->
  <url>
    <loc>{{ base_url }}/es/</loc>
    <lastmod>2025-10-15</lastmod> <!-- Actualiza a la fecha del último gran cambio -->
    <priority>1.0</priority>
  </url>
  <url>
    <loc>{{ base_url }}/en/</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>{{ base_url }}/es/blog/</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>0.9</priority>
  </url>
  <url>
    <loc>{{ base_url }}/en/blog/</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>0.9</priority>
  </url>

  <!-- === PÁGINAS DE SERVICIOS === -->
  <!-- Bucle para los servicios en ESPAÑOL -->
  {% for slug in servicios_es.keys() %}
  <url>
    <loc>{{ base_url }}/es/servicios/{{ slug }}</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>0.9</priority>
  </url>
  {% endfor %}
  <!-- Bucle para los servicios en INGLÉS -->
  {% for slug in servicios_en.keys() %}
  <url>
    <loc>{{ base_url }}/en/servicios/{{ slug }}</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>0.9</priority>
  </url>
  {% endfor %}

  <!-- === CASOS DE ESTUDIO === -->
  <!-- Bucle para los casos de estudio en ESPAÑOL -->
  {% for slug in casos_es.keys() %}
  <url>
    <loc>{{ base_url }}/es/casos-de-estudio/{{ slug }}</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>0.8</priority>
  </url>
  {% endfor %}
  <!-- Bucle para los casos de estudio en INGLÉS -->
  {% for slug in casos_en.keys() %}
  <url>
    <loc>{{ base_url }}/en/casos-de-estudio/{{ slug }}</loc>
    <lastmod>2025-10-15</lastmod>
    <priority>0.8</priority>
  </url>
  {% endfor %}

  <!-- === POSTS DEL BLOG === -->
  <!-- Bucle para los posts en ESPAÑOL -->
  {% for post in posts_es %}
  <url>
    <loc>{{ base_url }}/es/blog/{{ post.slug }}</loc>
    <lastmod>{{ post.date.strftime('%Y-%m-%d') }}</lastmod>
    <priority>0.7</priority>
  </url>
  {% endfor %}
  <!-- Bucle para los posts en INGLÉS -->
  {% for post in posts_en %}
  <url>
    <loc>{{ base_url }}/en/blog/{{ post.slug }}</loc>
    <lastmod>{{ post.date.strftime('%Y-%m-%d') }}</lastmod>
    <priority>0.7</priority>
  </url>
  {% endfor %}

</urlset>